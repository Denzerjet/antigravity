Input an integer between 0 and 511
Receiver will print a 3x3 grid of 0s and 1s accordingly

run receiver then sender

u can configure drops using grid.txt or change what is transferred in the grid

any drops are just the average of their adjacent cells, (including empty ones), taking the majority's value. 1 for tiebreakers.

assumes the grid is 0 and 1s
